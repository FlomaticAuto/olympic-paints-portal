# Supabase — olympic-paints-forms project
# Service role key bypasses RLS; NEVER expose to the browser.
SUPABASE_URL=https://nssufmvpdtzhybcqispv.supabase.co
SUPABASE_SERVICE_ROLE_KEY=__paste_service_role_key_here__

# iron-session cookie encryption key
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_PASSWORD=__paste_64_hex_chars_here__

# Optional — used by scripts/seed.ts to generate the admin user
ADMIN_USERNAME=quintus
ADMIN_FULL_NAME=Quintus Lategan
ADMIN_EMAIL=quintusl@olympicpaints.co.za
